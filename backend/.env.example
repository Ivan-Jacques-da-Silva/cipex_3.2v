
# Database Configuration
DATABASE_URL="postgresql://cipex_user:CipexPortal@2024!SecurePass@localhost:5432/cipex_portal"

# PostgreSQL Configuration (Replit)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=admin
POSTGRES_DB=cipex_portal
POSTGRES_USER=cipex_user
POSTGRES_PASSWORD=CipexPortal@2024!SecurePass

# Server Configuration  
PORT=3001

# Replit specific
REPLIT_DEV_DOMAIN=${REPLIT_DEV_DOMAIN}
